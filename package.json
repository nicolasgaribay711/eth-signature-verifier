{
  "name": "eth-signature-verifier",
  "version": "1.0.0",
  "description": "Smart contract for verifying Ethereum signatures and ownership claims",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost"
  },
  "keywords": [
    "ethereum",
    "signature",
    "verification",
    "ownership",
    "smart-contract"
  ],
  "author": "nicolasgaribay711",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.19.0",
    "ethers": "^6.9.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}